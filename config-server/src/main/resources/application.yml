server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ucsal/config-repo.git
          # branch: main  # opcional se usar branch main
management:
  endpoints:
    web:
      exposure:
        include: "*"